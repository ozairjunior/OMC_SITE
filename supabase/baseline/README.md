# Baseline V1 (em construção)

Esta pasta contém a primeira definição estrutural do schema V1 aprovada. Ela ainda não substitui as migrations históricas e não deve ser aplicada remotamente até que as RPCs, policies e testes de banco sejam concluídos.
